{
  "name": "dezoomify-cloudflare-worker",
  "version": "0.1.0",
  "description": "Dezoomify Cloudflare Worker proxy",
  "main": "worker/script.js",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml",
    "check": "wrangler deploy --dry-run --config wrangler.toml"
  },
  "keywords": [
    "cf",
    "worker"
  ],
  "author": "",
  "license": "GPL-2.0-or-later",
  "devDependencies": {
    "wrangler": "^3.83.0"
  },
  "engines": {
    "node": ">=18"
  }
}
